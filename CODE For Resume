<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Glitchy Resume Show</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: radial-gradient(circle at top, #0f172a, #020617);
      color: #e5e7eb;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .container {
      width: 90%;
      max-width: 900px;
      padding: 40px;
      background: rgba(2, 6, 23, 0.75);
      border-radius: 20px;
      box-shadow: 0 25px 80px rgba(0, 0, 0, 0.6);
      position: relative;
    }

    .glitch {
      font-size: 3rem;
      font-weight: 800;
      position: relative;
      color: #f8fafc;
      text-align: center;
      margin-bottom: 10px;
    }

    .glitch::before,
    .glitch::after {
      content: attr(data-text);
      position: absolute;
      left: 0;
      width: 100%;
      overflow: hidden;
      clip: rect(0, 900px, 0, 0);
    }

    .glitch::before {
      animation: glitchTop 2.5s infinite linear alternate-reverse;
      color: #22d3ee;
    }

    .glitch::after {
      animation: glitchBottom 1.8s infinite linear alternate-reverse;
      color: #a78bfa;
    }

    @keyframes glitchTop {
      0% { clip: rect(0, 9999px, 0, 0); transform: translate(0); }
      20% { clip: rect(0, 9999px, 20px, 0); transform: translate(-3px, -3px); }
      40% { clip: rect(10px, 9999px, 40px, 0); transform: translate(3px, 3px); }
      60% { clip: rect(5px, 9999px, 35px, 0); transform: translate(-2px, 2px);
